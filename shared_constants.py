"""Shared app-level constants used across multiple modules."""
_INTERRUPT_COMMANDS = {
    "@stop", "/stop",
    "@停止", "/停止",      # @停止 / 停止
    "@中断", "/中断",      # @中断 / 中断
    "@interrupt", "/interrupt",
}
