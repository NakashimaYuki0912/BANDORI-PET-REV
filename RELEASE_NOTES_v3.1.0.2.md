# BandoriPet v3.1.0.2 使用说明

> **基于 Live2D Cubism SDK 的桌面宠物应用**，让 BanG Dream! 角色以 Live2D 形态显示在桌面上。支持 LLM 驱动的 AI 角色扮演对话、TTS 语音合成、MCP/Computer Use 工具、多角色同时显示、外部聊天软件接入等功能。

---

## 📦 下载与安装

### 便携版（推荐）
1. 下载 `BandoriPet-3.1.0.2-WIN-AMD64.zip`
2. 解压到任意目录（**路径中不要包含中文/特殊字符**）
3. 双击 `BandoriPet.exe` 运行

### 安装版
- 下载 `BandoriPet-3.1.0.2-win32.msi`（如提供），按向导安装

### 依赖说明
- Windows 10/11 (64-bit)
- 已内置 Python 3.10 + 所有依赖，**无需额外安装**
- 如需 TTS：需单独下载 GPT-SoVITS 后端（可选）

---

## ✨ v3.1.0.2 更新内容

### 新增
- **托盘菜单「重置角色位置」**：一键将角色移回屏幕中央（水平+垂直居中）

### 修复
- **修复换服装/角色时预览越用越卡**：旧 Live2D 模型的 GL 贴图/缓冲不再泄漏到显存，长时间切换也不卡
- **修复天气查询空气泡**：天气工具支持中文城市名自动识别（合肥、南京等 37 城），模型未输出正文时退回工具结果兜底
- **修复右键菜单在屏幕边缘/角落错位**：宠物贴屏幕左右侧时，菜单自动切换为纵向堆叠布局，不再盖住宠物

---

## ⚠️ 必读：模型资源

程序**不包含模型文件**，首次运行需下载模型包：

1. 下载 `models.zip`（约 1GB，47 个角色 Live2D 模型）
2. 解压后把 `models/` 文件夹放到 `BandoriPet.exe` 同目录
3. 重启程序即可选择角色

---

## 🔧 首次配置（可选）

- **AI 对话**：在设置中填写 LLM API（OpenAI 兼容接口，支持 DeepSeek 等）
- **天气**：可选填心知天气凭据 + 默认城市 LocationID（如南昌 101240101）
- **TTS 语音**：可选配置 GPT-SoVITS 服务
